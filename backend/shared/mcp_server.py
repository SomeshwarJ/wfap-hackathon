# This file is no longer needed for the simplified approach
# We're using LangChain's built-in @tool decorator instead

# Placeholder file to avoid import errors
class SimpleMCPServer:
    def __init__(self, server_name: str = "wfap-agent-server"):
        pass

simple_mcp_server = SimpleMCPServer()