# WFAP Credit Negotiation System

A multi-agent AI system simulating agent-to-agent negotiation for credit offers using LLMs and MCP tools.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt